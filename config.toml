# 경로는 이 파일 기준 상대경로 또는 절대경로.
# 공유드라이브를 쓰려면 예) inbox = "G:/공유 드라이브/홍보자료/접수함"
inbox = "접수함"
done = "발행완료"
out = "out"

# 사이트 반영은 저장소 커밋이 아니라 어드민 API 로 한다 (prpub site).
# 교육후기가 Supabase cases 테이블 레코드라 저장소에 올릴 파일이 없다.
# 사이트 저장소는 구조 참조용이며 쓰지 않는다.
daeasy_repo = ""
